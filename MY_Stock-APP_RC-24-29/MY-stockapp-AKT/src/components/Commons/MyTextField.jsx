import TextField from "@mui/material/TextField";
import React from "react";
const MyTextField = (props) => {
  return <TextField style={{fontFamily:'monospace'}} {...props} />;
};

export default MyTextField;
